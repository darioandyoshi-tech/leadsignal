
# LeadSignal Dashboard

Next.js 15 + Tailwind + shadcn/ui.

## Setup

```bash
cd frontend
npx shadcn@latest init --yes --template next --base-color slate
npm install
npm run dev
```

## Features
- Signal list / filters
- Stats cards
- Subscription status + Stripe checkout
- Alert delivery settings
