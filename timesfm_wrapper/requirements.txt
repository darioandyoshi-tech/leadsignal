# Base TimesFM package (torch backend)
# Install inside the dedicated venv:
#   pip install -e /workspace/timesfm[torch]
# Then install this wrapper's lightweight extras:
fastapi>=0.110.0
pydantic>=2.0.0
typer>=0.12.0
uvicorn>=0.29.0
pandas>=2.0.0
numpy>=1.26.4
